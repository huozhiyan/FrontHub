import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        "sidebar-bg": "#545c64",
      },
    },
  },
  // 其他配置...
};

export default config;
